{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "cool-chart.serviceAccountName" . }}
  labels:
{{ include "cool-chart.labels" . | nindent 4 }}
{{- end -}}
