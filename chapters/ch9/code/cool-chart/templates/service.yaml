apiVersion: v1
kind: Service
metadata:
  name: {{ include "cool-chart.fullname" . }}
  labels:
    {{- include "cool-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cool-chart.selectorLabels" . | nindent 4 }}
